scalar BigDecimal
scalar LocalDateTime

interface Base {
    id: ID!
    createdAt: LocalDateTime!
    createdBy: String!
    updatedAt: LocalDateTime!
    updatedBy: String
}

enum CurrencyCode {
    USD
    EUR
    GBP
    CAD
    AUD
    JPY
}

enum Status {
    ACTIVE
    ARCHIVED
    CLOSED
}