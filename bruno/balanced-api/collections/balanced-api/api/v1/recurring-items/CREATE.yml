info:
  name: CREATE
  type: http
  seq: 4

http:
  method: POST
  url: "{{dripl-base-url}}/api/v1/recurring-items"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
