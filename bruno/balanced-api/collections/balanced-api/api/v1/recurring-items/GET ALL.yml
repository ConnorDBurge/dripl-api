info:
  name: GET ALL
  type: http
  seq: 3

http:
  method: GET
  url: "{{dripl-base-url}}/api/v1/recurring"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
