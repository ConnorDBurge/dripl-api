info:
  name: ONE
  type: http
  seq: 2

http:
  method: GET
  url: "{{dripl-base-url}}/api/v1/recurring-items/:recurringItemId"
  params:
    - name: recurringItemId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
