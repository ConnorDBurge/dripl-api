info:
  name: PATCH
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{dripl-base-url}}/api/v1/recurring-items/:recurringItemId"
  params:
    - name: recurringItemId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
