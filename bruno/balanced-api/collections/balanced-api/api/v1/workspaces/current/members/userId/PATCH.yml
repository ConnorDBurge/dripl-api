info:
  name: PATCH
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{dripl-base-url}}/api/v1/workspaces/current/members/:userId"
  params:
    - name: userId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
