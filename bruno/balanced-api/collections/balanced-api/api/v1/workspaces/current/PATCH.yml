info:
  name: PATCH
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{dripl-base-url}}/api/v1/workspaces/current"
  body:
    type: json
    data: |-
      {
          "name": "Third Family Workspace"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
