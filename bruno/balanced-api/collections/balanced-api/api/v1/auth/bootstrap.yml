info:
  name: /bootstrap
  type: http
  seq: 1

http:
  method: POST
  url: "{{balanced-base-url}}/api/v1/auth/bootstrap"
  body:
    type: json
    data: |-
      {
          "email": "connor.d.burge@gmail.com",
          "givenName": "Connor",
          "familyName": "Burge"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
