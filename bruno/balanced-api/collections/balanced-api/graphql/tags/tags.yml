info:
  name: tags
  type: graphql
  seq: 2

graphql:
  method: POST
  url: "{{balanced-base-url}}/graphql"
  body:
    query: |-
      query Tags {
        tags {
          id
          createdAt
          createdBy
          updatedAt
          updatedBy
          workspaceId
          name
          description
          status
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
