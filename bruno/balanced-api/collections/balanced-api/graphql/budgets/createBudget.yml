info:
  name: createBudget
  type: graphql
  seq: 4

graphql:
  method: POST
  url: "{{balanced-base-url}}/graphql"
  body:
    query: |-
      mutation CreateBudget($input: CreateBudgetInput!) {
        createBudget(input: $input) {
          id
          createdAt
          createdBy
          updatedAt
          updatedBy
          name
          anchorDay1
          anchorDay2
          intervalDays
          anchorDate
          accountIds
          currentPeriodStart
          currentPeriodEnd
        }
      }
    variables: |-
      {
        "input": {
          "name": "Bi-Weekly Budget",
          "anchorDate": "2026-03-06",
          "intervalDays": 14,
          "accountIds": ["50179f7b-92f3-480e-a767-3783307c80f8"]
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
